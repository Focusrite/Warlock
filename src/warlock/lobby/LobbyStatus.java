/**
 * File: warlock.lobby.LobbyStatus.java
 *
 * Currently unused, but when network is implemented this enum decides if the player can edit the
 * lobby or not (editing reserved for host only)
 */
package warlock.lobby;

public enum LobbyStatus {
   HOST, CLIENT
}
