/**
 * File: warlock.Handle.java
 *
 * An empty interface only utilized to check that the HandleLoader should actually run the init of
 * the class. Read below for info on why it's empty.
 */
package warlock;

public interface Handle {
   //This interface doesn't actually do anything since static methods can not be implemented into an
   //interface in java7 (version 8 has this!)
}
