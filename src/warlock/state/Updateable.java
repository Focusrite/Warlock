/**
 * File: warlock.state.Updateable.java
 *
 * An interface with the update(..) method utilized all over the project.
 */
package warlock.state;

public interface Updateable {
   public void update(double dt);
}
